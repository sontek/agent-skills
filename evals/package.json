{
  "name": "sontek-skills-evals",
  "private": true,
  "description": "Detection-rule regression + cross-language overfit suite (promptfoo). Run via `just` from the repo root.",
  "engines": {
    "node": "^20.20.0 || >=22.22.0"
  },
  "devDependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.700.0",
    "promptfoo": "0.121.15"
  }
}

Generic pre-rule guidance (the "before"): the only thing the agents said about
dead/speculative abstractions was a single design question in the code-reviewer,
with no fire-able rule, no verify step, and no exemptions.

- Are abstractions justified by current use, not speculative future use? Flag an
  abstraction that exists only for a hypothetical future need.

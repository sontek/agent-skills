Generic pre-rule guidance (the "before"): no rule for module/class sprawl. The
closest was the simplifier's general aim to avoid combining too many concerns,
with no heterogeneity test and no framework-aggregation exemption.

- Avoid combining too many concerns into single functions or components. Flag
  modules or classes that have grown too large or mix multiple responsibilities.

Generic pre-rule guidance (the "before"): the simplifier's general clarity aims
plus the code-reviewer's APOSD vocabulary — descriptive, with no depth/branch
threshold and no reducibility gate.

- Reduce unnecessary complexity and nesting. Prefer simpler control flow. Name
  cognitive load when a developer must hold too much to work in a function. Flag
  code that is unnecessarily complex.
